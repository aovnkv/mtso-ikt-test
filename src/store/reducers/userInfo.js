const initialState = {
  userData: {},
  isFetching: false,
  error: null
};
const userInfo = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userInfo;
