const initialState = {
  newsData: {},
  isFetching: false,
  error: null
};

const news = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default news;
