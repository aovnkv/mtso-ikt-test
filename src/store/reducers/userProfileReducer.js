const initialState = {
  userData: {},
  isFetching: false,
  error: null
};
const userProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userProfileReducer;
