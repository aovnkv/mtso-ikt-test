const initialState = {
    data: [],
    isFetching: false,
    error: undefined
};

function reduce(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default reduce;